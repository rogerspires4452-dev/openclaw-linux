# archiso practice: autologin root on the live TTY (PAM rejects empty passwords,
# so without this the live media login prompt can never accept root).
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin root --noclear %I $TERM
